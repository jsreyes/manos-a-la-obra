import { MultiFormatDirective } from './multi-format.directive';

describe('MultiFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiFormatDirective();
    expect(directive).toBeTruthy();
  });
});
